
# Test if the application is running on port 7258
port:
  tcp:8080:
    listening: true
    ip:
    - localhost

file:
  /wwwroot:
    filetype: directory
    exists: true

  # Check if the file `database.db` exists
  file:
    /database.db: 
      exists: true
